[build]
  publish = "."
  functions = "netlify/functions"

[[redirects]]
  from = "/api/download"
  to = "/.netlify/functions/download"
  status = 200
  force = true

[functions]
  node_bundler = "esbuild"
